
<div class="modal-dialog modal-md" role="document">
  <div class="modal-content">
    <div class="modal-header">
      <h4 class="modal-title <% if (type === 'error') { %>text-danger<% } else if (type === 'success') { %>text-success<% } %>" id="myModalLabel">
        <%= title %>
        <i class="glyphicon glyphicon-<% if (type === 'error') { %>warning-sign text-error<% } else if (type === 'success') { %>ok text-success<% } %>"></i>
      </h4>
    </div>
    <div class="modal-body">
      <div>
        <%= message %>
      </div>
      <div class='modal-actions'>
        <button type="button" class="btn btn-default" data-dismiss="modal">OK</button>
      </div>
    </div>
  </div>
</div>
